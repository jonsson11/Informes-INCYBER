diff --git a/versio_nova/pdf/generator.py b/versio_nova/pdf/generator.py
index 28a755b..5b40b73 100644
--- a/versio_nova/pdf/generator.py
+++ b/versio_nova/pdf/generator.py
@@ -612,17 +612,20 @@ def generate_pdf(company,stats,period_label,period_start=None,has_zabbix=False,z
         pdf.ln(3)
         start_x = pdf.get_x()
 
+        C_GREEN = (22, 163, 74)
+
         def _draw_zabbix_header():
             pdf.set_x(start_x)
             pdf.set_fill_color(*C_RED)
             pdf.set_text_color(*C_WHITE)
             pdf.set_font("helvetica", "B", 10)
-            pdf.cell(15, 11, "#", border=0, fill=True, align="C")
-            pdf.cell(40, 11, "Host", border=0, fill=True)
-            pdf.cell(65, 11, "Problema", border=0, fill=True)
-            pdf.cell(20, 11, "Ocurr.", border=0, fill=True, align="C")
-            pdf.cell(25, 11, "Severidad", border=0, fill=True, align="C")
-            pdf.cell(25, 11, "Última detección", border=0, fill=True, align="C",
+            pdf.cell(12, 11, "#", border=0, fill=True, align="C")
+            pdf.cell(33, 11, "Host", border=0, fill=True)
+            pdf.cell(53, 11, "Problema", border=0, fill=True)
+            pdf.cell(16, 11, "Ocurr.", border=0, fill=True, align="C")
+            pdf.cell(23, 11, "Severidad", border=0, fill=True, align="C")
+            pdf.cell(23, 11, "Estado", border=0, fill=True, align="C")
+            pdf.cell(30, 11, "Última detección", border=0, fill=True, align="C",
                 new_x=XPos.LMARGIN, new_y=YPos.NEXT,)
 
         _draw_zabbix_header()
@@ -668,9 +671,9 @@ def generate_pdf(company,stats,period_label,period_start=None,has_zabbix=False,z
                 pdf.rect(start_x, y0, 190, row_h_z, "F")
 
                 # Badge redondo con el numero de fila, igual que en Cuarentena/Top10
-                badge_cx = start_x + 7.5
+                badge_cx = start_x + 6
                 badge_cy = y0 + row_h_z / 2
-                badge_r = 3.6
+                badge_r = 3.4
                 pdf.set_draw_color(203, 213, 225)
                 pdf.set_line_width(0.25)
                 pdf.ellipse(badge_cx - badge_r, badge_cy - badge_r, badge_r * 2, badge_r * 2, "D")
@@ -678,29 +681,38 @@ def generate_pdf(company,stats,period_label,period_start=None,has_zabbix=False,z
                 pdf.set_text_color(*C_GRAY)
                 pdf.set_font("helvetica", "B", 8)
                 pdf.set_xy(start_x, badge_cy - 2.3)
-                pdf.cell(15, 4.6, str(pos), align="C")
+                pdf.cell(12, 4.6, str(pos), align="C")
 
                 pdf.set_text_color(*C_TEXT)
                 pdf.set_font("helvetica", "", 8)
                 text_y = y0 + (row_h_z - 5) / 2
 
-                pdf.set_xy(start_x + 15, text_y)
-                pdf.cell(40, 5, str(p["host"])[:24])
+                pdf.set_xy(start_x + 12, text_y)
+                pdf.cell(33, 5, str(p["host"])[:20])
 
-                pdf.set_xy(start_x + 55, text_y)
-                pdf.cell(65, 5, str(p["name"])[:44])
+                pdf.set_xy(start_x + 45, text_y)
+                pdf.cell(53, 5, str(p["name"])[:35])
 
                 pdf.set_font("helvetica", "B", 8)
-                pdf.set_xy(start_x + 120, text_y)
-                pdf.cell(20, 5, str(p["occurrences"]), align="C")
+                pdf.set_xy(start_x + 98, text_y)
+                pdf.cell(16, 5, str(p["occurrences"]), align="C")
 
                 pdf.set_font("helvetica", "", 8)
-                pdf.set_xy(start_x + 140, text_y)
-                pdf.cell(25, 5, str(p["severity"]), align="C")
+                pdf.set_xy(start_x + 114, text_y)
+                pdf.cell(23, 5, str(p["severity"]), align="C")
+
+                resolved = p.get("resolved", False)
+                estado_str = "RESUELTO" if resolved else "ACTIVO"
+                pdf.set_text_color(*C_GREEN if resolved else C_RED)
+                pdf.set_font("helvetica", "B", 7.5)
+                pdf.set_xy(start_x + 137, text_y)
+                pdf.cell(23, 5, estado_str, align="C")
 
+                pdf.set_text_color(*C_TEXT)
+                pdf.set_font("helvetica", "", 8)
                 fecha_str = p["date"].strftime("%d/%m/%y") if p.get("date") else "N/D"
-                pdf.set_xy(start_x + 165, text_y)
-                pdf.cell(25, 5, fecha_str, align="C")
+                pdf.set_xy(start_x + 160, text_y)
+                pdf.cell(30, 5, fecha_str, align="C")
 
                 sep_ys.append(y0 + row_h_z)
                 pdf.set_xy(start_x, y0 + row_h_z)
diff --git a/versio_nova/reports/zabbix_problems.py b/versio_nova/reports/zabbix_problems.py
index bbf16eb..34da6c0 100644
--- a/versio_nova/reports/zabbix_problems.py
+++ b/versio_nova/reports/zabbix_problems.py
@@ -46,7 +46,14 @@ def fetch_suppressed_acknowledged_problems(
     SUPRIMIDOS + RECONOCIDOS (ya tratados: ticket creado / en resolución),
     agrupados por (host, problema) con un contador de 'occurrences' y la
     fecha de la detección MÁS RECIENTE, ordenados de más reciente a más
-    antiguo.
+    antiguo. Cada grupo incluye también 'resolved' (bool): si la ocurrencia
+    más reciente del grupo ya se resolvió en Zabbix o sigue activa.
+
+    Se usa event.get en lugar de problem.get: problem.get solo devuelve
+    problemas activos (o resueltos muy recientemente, según el ajuste
+    "Display OK triggers for N seconds" de Zabbix), así que en una ventana
+    de un mes se perderían los que ya se cerraron. event.get sí mantiene
+    el histórico completo filtrado por fecha.
 
     Si se pasan time_from/time_till, se filtra por el periodo del informe
     (mismo periodo que BD_REPORT_INTERVAL usa para BitDefender).
@@ -77,6 +84,7 @@ def fetch_suppressed_acknowledged_problems(
         "groupids": group_ids,
         "acknowledged": True,
         "suppressed": True,
+        "value": 1,  # solo eventos de tipo "problema" (no los de "OK"/recuperación)
         "output": "extend",
         "selectTags": "extend",
         "sortfield": "eventid",
@@ -89,13 +97,13 @@ def fetch_suppressed_acknowledged_problems(
     if ts_till is not None:
         params["time_till"] = ts_till
 
-    r = api_call("problem.get", params)
+    r = api_call("event.get", params)
     raw_problems = r.get("result", [])
     if not raw_problems:
         return []
 
-    # problem.get no devuelve el host directamente: solo el 'objectid' del
-    # trigger que generó el problema. Hay que resolver host por trigger
+    # event.get no devuelve el host directamente: solo el 'objectid' del
+    # trigger que generó el evento. Hay que resolver host por trigger
     # aparte con trigger.get (object == "0" significa que el problema
     # viene de un trigger, que es el caso normal).
     trigger_ids = list({
@@ -118,7 +126,7 @@ def fetch_suppressed_acknowledged_problems(
 
     # --------------------------------------------------
     # Agrupar por (host, problema): mismo problema en la misma
-    # máquina varias veces -> "occurrences" + fecha de la última.
+    # máquina varias veces -> "occurrences" + fecha/estado de la última.
     # --------------------------------------------------
     grouped = OrderedDict()
 
@@ -127,6 +135,9 @@ def fetch_suppressed_acknowledged_problems(
         name = p.get("name")
         severity = ZABBIX_SEVERITY_MAP.get(p.get("severity"), "Desconocida")
         clock = datetime.fromtimestamp(int(p["clock"])) if p.get("clock") else None
+        # r_eventid == "0" -> el evento sigue sin resolver (aún activo).
+        # cualquier otro valor -> ya existe un evento de recuperación.
+        resolved = p.get("r_eventid", "0") != "0"
 
         key = (host_name, name)
 
@@ -137,12 +148,14 @@ def fetch_suppressed_acknowledged_problems(
                 "severity": severity,
                 "date": clock,
                 "occurrences": 1,
+                "resolved": resolved,
             }
         else:
             grouped[key]["occurrences"] += 1
             if clock and (grouped[key]["date"] is None or clock > grouped[key]["date"]):
                 grouped[key]["date"] = clock
                 grouped[key]["severity"] = severity
+                grouped[key]["resolved"] = resolved
 
     problems = sorted(
         grouped.values(),
diff --git a/versio_nova/test_zabbix.py b/versio_nova/test_zabbix.py
index ae21987..b23042e 100644
--- a/versio_nova/test_zabbix.py
+++ b/versio_nova/test_zabbix.py
@@ -23,4 +23,5 @@ if __name__ == "__main__":
         print(f"Se han encontrado {len(problems)} problemas:\n")
         for p in problems:
             fecha = p["date"].strftime("%d/%m/%Y %H:%M") if p["date"] else "N/D"
-            print(f"  - [{p['severity']}] {p['host']} | {p['name']} | {fecha}")
\ No newline at end of file
+            estado = "RESUELTO" if p.get("resolved") else "ACTIVO"
+            print(f"  - [{p['severity']}] {p['host']} | {p['name']} | x{p['occurrences']} | {estado} | {fecha}")
\ No newline at end of file
